import styled from "styled-components";

export const BoxForm: any = styled.div`
  padding: 120px 60px 36px 60px;

  border: 2px solid ${(p) => p.theme.colors.g};
  border-radius: ${(p) => p.theme.radii.inputAuthForm};
`;
